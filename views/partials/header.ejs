<header class="navbar">
  <h1>URL Shortener</h1>
  <% if (user) { %>
    <a href="/logout" class="btn logout">Logout</a>
  <% } else { %>
    <a href="/login" class="btn">Login</a>
  <% } %>
</header>
